### Codes for ruled based reconciliation

    ruleBasedMatcher.py
Python implementation for matching person entries to external databases, see code comments.

    match_people.ipynb
Code for testing the reconciliation. 
NB. to be later replaced with py-code runnable from command line including input parameters.


Setup:

```
python -m venv .env
pip install -r requirements.txt
```